import {
  type EndpointRuntimeDefinition,
  type ToolHandlerContext,
  invokeEndpoint,
} from "../runtime.js";

const ENDPOINT: EndpointRuntimeDefinition = {{{json tool.endpoint}}};

export async function {{tool.handlerFunctionName}}(
  input: Record<string, unknown>,
  context: ToolHandlerContext,
): Promise<unknown> {
  return invokeEndpoint(ENDPOINT, input, context);
}
