# MCP Server for {{apiName}}

Expose {{apiName}} as MCP tools over stdio with a generated runtime that can be verified locally before live calls.

Generated by **mcpforge** at {{generatedAt}}.

## Tool Surface

- Public tools: {{tools.length}}
- Workflow tools: {{workflowToolCount}}
- Endpoint tools: {{endpointToolCount}}

Workflow tools are deterministic task wrappers that execute one or more upstream API calls. Endpoint tools are direct HTTP wrappers for specific operations.

## Prerequisites

- Node.js 18+
- npm 9+

## Setup

1. Install dependencies:

```bash
npm install
```

2. Configure environment:

```bash
cp .env.example .env
```
{{#if hasAuth}}
{{#if (eq auth.type "oauth2")}}
{{#if authRequired}}
OAuth credentials are required for authenticated endpoints.
{{else}}
OAuth credentials are optional and only needed for authenticated endpoints.
{{/if}}

For OAuth-backed APIs, you can either set `{{auth.envVarName}}` to a current access token or configure token acquisition with `OAUTH_TOKEN_URL` plus `OAUTH_CLIENT_ID` / `OAUTH_CLIENT_SECRET` for client credentials. If you have a refresh token, set `OAUTH_REFRESH_TOKEN`; the generated runtime will renew access tokens in memory before calls.
{{else}}
{{#if authRequired}}
The `{{auth.envVarName}}` value is required for authenticated endpoints.
{{else}}
The `{{auth.envVarName}}` value is optional and only needed for authenticated endpoints.
{{/if}}
{{/if}}
{{/if}}

3. Build:

```bash
npm run build
```

4. Verify request compatibility:

```bash
npx mcpforge test
```

This runs the generated server against a local mock upstream and verifies path, query, header, auth, and request-body handling for the supported compatibility matrix.

5. Run:

```bash
npm start
```

## Claude Desktop Configuration

Add this to your Claude Desktop MCP config file and replace the absolute path:

```json
{
  "mcpServers": {
    "{{projectName}}": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/{{projectName}}/dist/index.js"],
      "env": {
        "API_BASE_URL": "{{baseUrl}}"{{#unless (eq auth.type "none")}},
        "{{auth.envVarName}}": "replace-me"{{/unless}}
      }
    }
  }
}
```

## Cursor Configuration

Add this to `.cursor/mcp.json` or your global Cursor MCP config:

```json
{
  "mcpServers": {
    "{{projectName}}": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/{{projectName}}/dist/index.js"],
      "env": {
        "API_BASE_URL": "{{baseUrl}}"{{#unless (eq auth.type "none")}},
        "{{auth.envVarName}}": "replace-me"{{/unless}}
      }
    }
  }
}
```

## Available Tools

| Tool | Kind | Route | Description |
| --- | --- | --- | --- |
{{#each tools}}
{{#if (eq this.kind "workflow")}}
| `{{this.name}}` | `workflow` | `workflow` | {{this.description}} |
{{else}}
| `{{this.name}}` | `endpoint` | `{{this.endpoint.method}} {{this.endpoint.path}}` | {{this.description}} |
{{/if}}
{{/each}}

## Supported Compatibility Matrix

- Auth: header/query/cookie API keys, bearer tokens, basic auth, OAuth client credentials, and OAuth refresh-token renewal.
- Request bodies: `application/json`, `application/x-www-form-urlencoded`, `multipart/form-data`, text payloads, and binary-compatible bodies.
- Verification: `npx mcpforge test` validates generated request construction against a local mock upstream before you switch to live calls.
- OAuth notes: browser-based authorization-code login is not generated. Use an existing refresh token, a client-credentials app, or a ready-to-use access token in `ACCESS_TOKEN`.

## Troubleshooting

- Authentication errors (`401` / `403`):
{{#if hasAuth}}
  - Verify `.env` contains `{{auth.envVarName}}` and the value format is correct.
{{else}}
  - This API was detected as unauthenticated. Verify the upstream service really does not require credentials in your environment.
{{/if}}
- API unreachable or timeout:
  - Check `API_BASE_URL`, network access, and whether the upstream API is reachable from your machine.
- Tool call fails with validation-style errors:
  - Ensure required path parameters, query parameters, headers, and request body fields are present.
- Compatibility verification fails:
  - Run `npx mcpforge test` and review which request field mismatched. Most failures are incorrect auth env vars or unsupported payload shapes for the upstream API.
- Workflow tool fails:
  - Run `npx mcpforge diff` or `npx mcpforge update` from the source project to check whether the upstream API changed under a workflow dependency.
- Server does not appear in the MCP client:
  - Confirm the config path points at `dist/index.js`, then restart the MCP client.
- OAuth-managed APIs:
  - For client credentials, set `OAUTH_TOKEN_URL`, `OAUTH_CLIENT_ID`, and `OAUTH_CLIENT_SECRET`.
  - For refresh-token renewal, set `OAUTH_TOKEN_URL` and `OAUTH_REFRESH_TOKEN`. Some providers also require `OAUTH_CLIENT_ID` and `OAUTH_CLIENT_SECRET`.
